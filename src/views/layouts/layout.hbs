<!DOCTYPE html>
<html>
  <head>
    <!-- Page character encoding -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{title}}</title>

 <!-- Bulma CSS framework for styling -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
   
    <!-- Leaflet CSS for map styling -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />

<!-- Layout Template -->
  <style>
    body { background: #fff9fc; }
    .hero-gradient { background: linear-gradient(135deg, #fff0f7, #fffafc); }
    .pink-card { border: 1px solid #f3d7e6; border-radius: 16px; box-shadow: 0 10px 20px rgba(0,0,0,0.05); }
    .map-box { height: 420px; border-radius: 16px; overflow: hidden; }
    .tag-soft { background: #ffe3ef; color: #a3366c; } .public-salon-card { border-radius: 18px;
    transition: 0.3s ease;} .public-salon-card:hover { transform: translateY(-4px); box-shadow: 0 12px 30px rgba(0,0,0,0.12);
  }
  </style>

  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
  </head>
  <body>
      {{{content}}}
  </body>
</html>
